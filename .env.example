# Environment configuration for RetroToolbox server

# Node environment (development/production)
NODE_ENV=production

# Server port (default: 3004)
PORT=3004

# Allowed origins for CORS (comma-separated)
# Example: https://yourdomain.com,https://app.yourdomain.com
ALLOWED_ORIGINS=

# Tailscale IPs for CORS (comma-separated)
# Example: 100.x.x.x,100.y.y.y
TAILSCALE_IPS=